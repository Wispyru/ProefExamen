/// <summary>
/// Defines all ingredient types used in the Match Minigame.
/// Each value corresponds to a tile type that can appear on the grid.
/// Add new ingredients here only — all other systems read from this enum dynamically.
/// </summary>
public enum IngredientType
{
    RawMeat,
    Cheese,
    Eggs,
    Soda,
    Milk,
    Vegetables,
    Fruit,
    Dough,
    Sugar
}